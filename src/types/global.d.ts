export {}
declare global {
  interface Project {
    title: string
    month: string
    link: string
    img: string
    desc: string
    time: string
  }
  interface Position {
    x: number
    y: number
  }
}
